<nav class="nav">
    {% set navPages = collections.all | eleventyNavigation %}
    <ul class="nav-list" role="list">
        {%- for entry in navPages %}
        <li>
            <a href="{{ entry.url | url }}" {% if entry.url==page.url %} data-current="current item" {% endif %}>{{
                entry.title }}</a>
            {% if entry.url==page.url %}
            {% set postslist = collections[page.fileSlug] %}
            {% if page.fileSlug == "books" %}
            {% set postslist = [
            {
                url: "#text-och-illustration",
                data: {
                    title: "Text och illustration"
                }
            },
            {
                url: "#grafisk-form",
                data: {
                  title: "Grafisk form"
                }
            },
            {
                url: "#datorprogram-för-barn-med-funktionsvariationer",
                data: {
                    title: "Datorprogram för barn med funktionsvariationer"
                }
            },
            {
                url: "#akademiskt-arbete",
                data: {
                    title: "Akademiskt arbete"
                }
            }
            ] %}
            {% endif %}
            <ul role="list">
                {% for post in postslist %}
                <li>
                    <a href="{{ post.url | url }}">
                        {% if post.data.year %}
                        <time datetime="{{ post.data.year }}">
                            {{ post.data.year }}
                        </time>
                        {% endif %}
                        {% if post.data.title %}
                            {{ post.data.title }}
                        {% else %}
                            Untitled
                        {% endif %}
                    </a>
                </li>
                {% endfor %}
            </ul>
            {% endif %}
        </li>
        {%- endfor %}
    </ul>
</nav>